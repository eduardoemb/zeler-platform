# mongo.env.template — MongoDB init credentials
# Populated at boot by zeler-platform-secrets.sh from Secret Manager.
# NO BASE env vars — mongo container only needs init credentials.

MONGO_INITDB_ROOT_USERNAME=__PLACEHOLDER__
MONGO_INITDB_ROOT_PASSWORD=__PLACEHOLDER__
