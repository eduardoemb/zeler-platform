[project]
name = "zeler-publicador"
version = "0.0.0"
description = "Publicador module package"
requires-python = ">=3.11"
dependencies = ["zeler_platform_core", "fastapi>=0.110,<1", "uvicorn[standard]>=0.30.0", "motor>=3.7.1"]

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zeler_publicador"]

[tool.uv.sources]
zeler_platform_core = { workspace = true }
