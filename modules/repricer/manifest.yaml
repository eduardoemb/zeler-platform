name: repricer
version: 0.1.0
subscribed_events:
  - items.*
  - items_prices.*
owned_collections:
  - repricer_rules
  - repricer_history
allowed_meli_scopes:
  - PUT /items/*
  - GET /items/*
  - GET /items/*/prices
health_endpoint: /health
