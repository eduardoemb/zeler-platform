FROM python:3.11-slim AS runtime

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PORT=8080

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock ./
COPY core ./core
COPY modules/autoreply ./modules/autoreply

RUN uv sync --frozen --package zeler-autoreply --no-dev

RUN test -f .venv/bin/uvicorn

RUN useradd --system --create-home --uid 1001 appuser
RUN chown -R appuser:appuser /app
USER appuser

CMD ["sh", "-c", ".venv/bin/uvicorn zeler_autoreply.app:make_app --factory --host 0.0.0.0 --port ${PORT:-8080}"]
